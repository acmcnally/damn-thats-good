/**
 * @dtg/db — Drizzle schema and migrations, the source of truth for the data model.
 *
 * Drizzle + Postgres wiring and the first migration land with DAMN-26; the
 * recipe / version schema itself is owned by DAMN-2.
 */

/** Placeholder export so the package resolves and has something to test. */
export const DB_PACKAGE = '@dtg/db';
