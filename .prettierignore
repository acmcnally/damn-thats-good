dist
coverage
pnpm-lock.yaml

# Drizzle-generated migrations + metadata — owned by drizzle-kit, not hand-formatted
**/drizzle/

# Prose docs (ADRs, guides) are authored by hand — Prettier's Markdown
# normalization (emphasis markers, list style) is churn without benefit here.
*.md

# Design mockups — throwaway artifacts from the ux-ui design workflow
# (single-file HTML + captured PNGs), not hand-formatted source.
.mockups/
docs/features/**/mockups/
